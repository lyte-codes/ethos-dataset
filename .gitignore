__pycache__/
*.pyc
.venv/
venv/

checkpoints/
*.safetensors
*.bin

*.failures.log
*.log
smoke_test.jsonl
verify_*.jsonl

# Rendered call audio and the transcripts beside it. Regenerable from the call
# JSON, and an MP3 per call would bloat the history fast.
data/audio/
data/fake_calls.json

.DS_Store

# handoff inbox — see NEXT.md
NEXT.md.bak
NEXT.md

# Real-call feedback. Everything under private/ stays on this machine: it is derived
# from actual calls, so it contains real names and real phone numbers. The public repo
# ships the code and the synthetic data, never this.
private/
logs/
